/**
 * Enum for contextual top bar action template ref
 * describes the position in which each type of button should be inserted
 * @note only for start/end positions
 */
export enum Uk2ContextualTopBarActionTemplateRef {
  backButton,
  closeButton,
}
